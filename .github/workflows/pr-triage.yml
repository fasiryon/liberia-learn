name: PR Triage

on:
  push:

jobs:
  triage-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: PR triage temporarily disabled
        run: echo "PR triage temporarily disabled while workflow syntax is repaired"
