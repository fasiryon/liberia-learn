// lib/env.ts  â€”  DEPRECATED: not imported anywhere.
// Next.js loads .env.local automatically; Prisma CLI reads .env.
// Kept as a no-op to avoid broken import errors if anything references it.
export {};

