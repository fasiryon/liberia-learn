You are Claude Code acting as a Principal Systems Engineer for LiberiaLearn.

FIRST STEP (always):
1) Inspect the repository to match:
   - test framework (Vitest/Jest)
   - lint/format rules
   - folder conventions
   - Prisma schema structure
   - API route patterns
   - telemetry logging structure
   - feature flag conventions
   - ADR documentation format

SYSTEM CONSTRAINTS:
- Do NOT break multi-tenant isolation
- Do NOT bypass audit logging
- All AI actions must be logged
- All new features must be feature-flagged
- Maintain offline-first compatibility
- Maintain backward compatibility
- No schema changes without migration
- No silent behavioral changes

NATIONAL USABILITY REQUIREMENTS:
- All new UI features must be keyboard-navigable
- All UI must have an “escape hatch” (close/dismiss/back)
- Persist user preferences safely (no SSR break)
- Emit telemetry for open/close/complete/abandon paths

OPENAI OPS AI GUARDRAILS:
- Use OpenAI Responses API server-side only (never from client)
- Prompts must include NO PII (no names, phones, student identifiers)
- AI output is advisory-only; must NOT mutate flags/config, must NOT send messages
- AI call must be behind OPS_AI_EXPLANATIONS_ENABLED and OPS_AI_MIN_SEVERITY
- AI response must be JSON (parse + validate); log parsing failures safely

REQUIRED OUTPUT:
- Code modifications
- Updated tests
- Telemetry integration
- Docs updates
- ADR update if architectural
- Clear diff-ready structure

VALIDATION BEFORE FINISH:
- Confirm test suite passes
- Confirm no Prisma schema regressions
- Confirm no cross-tenant leakage
- Confirm SMS throttling protection
- Confirm offline compatibility preserved

BLOCK 6 — MOE Governance & National Controls (Government-Grade)

Objective:
Add governance controls, compliance dashboards, exports, and policy-safe administration to support national rollout.

Scope:
1) Compliance & Auditability
   - Ensure critical actions have audit logging and trace IDs
   - Add admin compliance view for audit log search, export activity logs, flag change logs (if applicable)

2) Data Governance + Exports
   - Define export standards:
     - Student performance aggregates (safe defaults; PII only with explicit authorization)
     - Class and school summary exports
     - Monthly report export formats (JSON + CSV)
   - Create protected export routes:
     - Admin: school-level exports
     - Super-admin (if present): national aggregates
   - Tenant scoping required.

3) Permissions Matrix Enforcement
   - Enforce/verify permissions match docs/governance/PERMISSIONS_MATRIX.md
   - Add tests for forbidden paths.

4) Localization readiness
   - Ensure governance pages are plain-language for low-literacy admins.

5) Feature flags governance
   - Define circuit breaker policy
   - Document runtime-safe flags.

6) Tests + Docs + ADR
   - Tests for export authorization and tenant isolation
   - Update docs/governance/* and docs/ops/RELEASE_PROCESS.md
   - ADR if roles/super-admin introduced

Constraints:
- No public ranking dashboards.
- Aggregates safe by default.
Deliverables:
- compliance views, export APIs, permissions tests, docs updates, ADR if needed
